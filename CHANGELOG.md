# CHANGELOG — история версий Consilium Spine

Один дом для чисел о собственной работе. Заведён 6 августа 2026 после того, как
аудиторский пакет ушёл к внешним рецензентам с цифрами «477 тестов и 108 мутаций»,
которых ни в одной версии не существовало.

---

## Правила ведения — обязательны

1. **Строка добавляется в тот же чейнджсет, что и версия.** Не после, не пачкой.
2. **Числа берутся с выхода команд, не из памяти и не из другого документа.**
   Источник — ровно эти три:
   ```
   npm install
   python3 run_tests.py            → строка "Ran N tests" + "ALL GREEN" + exit-код
   python3 tools/mutation_probe.py → строка "catalogue: N cases, N RED, N SKIP, N GREEN"
   ```
3. **Базис едет с числом.** Измеренное на прогоне и посчитанное статически по файлам —
   разные величины и помечаются по-разному. Сравнивать их между собой нельзя.
4. **Неизвестное называется неизвестным.** Пустая клетка запрещена; пишется
   `НЕ ВОССТАНОВЛЕНО` с причиной.
5. **Строки не переписываются.** Ошибку в прошлой строке исправляет новая строка
   с пометкой, а не правка старой.

---

## Версии

### v4.2.83 — 2026-08-05, запись 2026-08-08

Строка добавлена с опозданием на два дня. Причина названа, потому что правило 1 нарушено:
`CHANGELOG.md` был заведён 6 августа, когда чейнджсет v4.2.83 уже был собран и передан
оператору. Исполнитель о существовании файла не знал. Это объяснение, а не оправдание:
следующая версия получает строку тем же чейнджсетом.

**Проверки (ИЗМЕРЕНО, прогон 8 августа из копии, скачанной с GitHub, а не из рабочего дерева):**

| Величина | Значение |
|---|---|
| Python-тестов исполнено | **490** (пропущено 0) |
| `run_tests.py` | `ALL GREEN — safe to deploy`, exit-код 0 |
| Каталог мутаций | **108 кейсов, 108 RED, 0 SKIP, 0 GREEN, 0 NOT_EXEC** |
| `mutation_probe.py` | `all probed pins went RED`, exit-код 0 |
| Распределение кейсов по раннерам (СТАТИЧ., подсчёт селекторов в файле) | 60 в node, 48 в unittest |

Базис распределения — статический разбор селекторов каталога, не замер прогона; совпадает
с числом, полученным архитектором независимо. Прежде это распределение было бессмысленным:
до v4.2.83 все 108 селекторов уходили в один раннер.

**Что изменилось — изменённые файлы:**

| Файл | Что |
|---|---|
| `microservice/ivc_lib.py` | сторож долга читает `_edgar.divergence.total_debt`, а не только верхний уровень |
| `microservice/*.py` (девять остальных) | только маркер сборки `v4.2.82` → `v4.2.83` |
| `workflow/consilium_spine_v4_2_83.json` | новый; `..._v4_2_82.json` удалён |
| `tests/test_harness.py` | фикстура ORCL приведена к настоящей форме, +3 пина по расхождению |
| `tests/test_brief_render.js` | пины мандата 07; четыре прежних пина переписаны с формулировок на свойства |
| `tests/test_probe_reports_execution.py` | новый, 10 пинов к самому пробнику |
| `tools/mutation_probe.py` | `.js` через node; число исполненных проверок из вывода селектора; состояние `NOT_EXEC`; +5 кейсов; 4 пустых кейса починены |
| `tools/build_handoff.py` | пятый счётчик и отказ собирать пакет при `NOT_EXEC` |
| `docs/HANDOFF_...md` | секция OOOOO |
| `handoff/CHAT_CONSILIUM_SEED_v2.md` | реестр правил 21–23, метаданные из сборщика |

По содержанию: выходной множитель печатается одним форматом из одного помощника
(в записке ORCL он стоял как `28` и как `27.53` в соседних ячейках, и `14.27 × 28` было
напечатано над результатом `392.85`); отсутствие русского названия сценария помечается
на самой строке; метрика листьев публикует три состояния вместо двух.

**Рост 481 → 490 — объяснение по правилу 3, и оно НЕ является ростом одного ряда.**

Числа `481` и `490` измерены на **двух разошедшихся ветках**, а не на соседних точках
одной. Сложение их разности бессмысленно, и вот почему:

| | тесты | каталог |
|---|---|---|
| v4.2.82, состояние на 5 августа — точка ветвления, из которой собран v4.2.83 | **477** | 103 |
| ↳ ветка A (эта, v4.2.83): +3 пина по расхождению долга (`test_harness.py`), +10 пинов к пробнику (`test_probe_reports_execution.py`) | **490** | 108 |
| ↳ ветка B (папка `CONSILIUM_REPO_v4_2_82` сегодня): +4 пина `TestPeSectorMedianAbsentFlag` (`test_harness.py`), +1 кейс `secmed-01` | **481** | 104 |

**Следствие, которое обязано быть записано здесь: v4.2.83 НЕ содержит работу ветки B.**
Флаг `pe_sector_median_absent` в `app.py`, класс `TestPeSectorMedianAbsentFlag` из четырёх
пинов и кейс мутации `secmed-01` в v4.2.83 отсутствуют — проверено грепом по трём файлам,
все три дают ноль. Чейнджсет «Issue #12» приземлился в папку v4.2.82 после того, как
v4.2.83 был собран, и слияния не было. Принимая v4.2.83, система теряет этот чейнджсет.

**Попутно: числа `477` и `108` из аудиторского пакета существовали, просто не вместе.**
`477` — прогон принятого дерева v4.2.82 5 августа, до чейнджсета Issue #12;
`108` — каталог v4.2.83 после починки маршрутизации пробника. Как пара они не жили
ни в одной версии, и в этом смысле запись ниже верна; как отдельные величины —
каждая измерена и адрес каждой назван. Строку v4.2.67–v4.2.81 не переписываю
(правило 5); настоящая строка её уточняет.

**Открыто на момент записи:** v4.2.83 не задеплоен, решение отложено сознательно;
чейнджсет Issue #12 не слит в v4.2.83; §5 мандата 07 и чейнджсет Grok не начаты;
четыре пустых пина каталога (`prose-01`, `three-02`, `d2-02`, `d9-01` покраснели сами
после переписывания их поверхностей — остальные четыре починены поимённо); правило
терминального множителя и ставки заморожены до итогов исторической проверки.

---

### v4.2.82 — 2026-08-06

**Проверки (ИЗМЕРЕНО, прогон 6 августа):**

| Величина | Значение |
|---|---|
| Python-тестов исполнено | **481** (пропущено 0) |
| Файлов тестов, попадающих под discovery | 14 |
| JS-ворот исполнено | 13 из 13 (ни одного «не удалось запустить») |
| Счётных утверждений в JS-воротах | 221 |
| Узлов кода: парсинг / разрешение идентификаторов | 37 из 37 / 37 из 37 |
| Каталог мутаций | **104 кейса, 104 RED, 0 SKIP, 0 GREEN** |
| `run_tests.py` | `ALL GREEN — safe to deploy`, exit-код 0 |
| `mutation_probe.py` | `all probed pins went RED`, exit-код 0 |
| `npm install` | 99 пакетов; отчёт npm: 1 уязвимость high severity |

**Что изменилось:** НЕ ВОССТАНОВЛЕНО в полном объёме — файл заведён позже самой версии.
Установлено по диффу с v4.2.81: затронуто 1 130 строк из 26 722 (без файла воркфлоу);
изменены `ivc_lib.py`, `app.py`, `edgar_facts.py`, `pricing.py`, `run_tests.py`,
`mutation_probe.py`, `build_handoff.py`, три файла тестов, `CLAUDE.md` и сид Консилиума.
По содержанию пинов v4.2.82: отказ по балансу виден в человеческой записке; секция
STREET печатает расстояние до консенсуса; крупный разрыв с консенсусом называется
явно; имена сценариев по-русски с видимым запасным вариантом.

**Открыто на момент записи:** инцидент с долгом не закрыт (ветка расхождения источников
не проверена на живых данных); валидационная матрица пройдена только на четырёх
однотипных крупных технологических компаниях; качественный слой даёт один содержательный
раздел из четырнадцати.

---

### v4.2.67 — v4.2.81

**Что изменилось: НЕ ВОССТАНОВЛЕНО.** Причина: пофайловой записи изменений в проекте
не велось, а история решений живёт в архивах чатов и в `docs/HANDOFF_ARCHITECT_*`.
Восстанавливать задним числом по памяти запрещено правилом 2 — это ровно тот класс
ошибки, из-за которого файл и заведён.

Что удалось измерить **статически** по файлам репозитория (это НЕ прогон: считаются
объявленные `def test_` и записи каталога мутаций; исполненное число может отличаться,
и в эпоху дефекта самопропуска отличалось):

| Версия | `def test_` (статич.) | Кейсов мутаций (статич.) |
|---|---|---|
| v4.2.67 | 451 | 41 |
| v4.2.68 | 457 | 44 |
| v4.2.69 | 457 | 47 |
| v4.2.72 | 459 | 54 |
| v4.2.73 | 459 | 54 |
| v4.2.74 | 459 | 57 |
| v4.2.75 | 459 | 57 |
| v4.2.76 | 459 | 57 |
| v4.2.77 | 470 | 72 |
| v4.2.78 | 470 | 73 |
| v4.2.79 | 473 | 81 |
| v4.2.80 | 473 | 92 |
| v4.2.81 | 473 | 96 |
| v4.2.82 | 481 | 104 | (сверено с прогоном: совпало)

Ряд монотонный. Числа 477 и 108, напечатанные в аудиторском пакете, в нём отсутствуют.

**Каталогов v4.2.70 и v4.2.71 в репозитории нет.** Причина не установлена: версии либо
не выкладывались, либо были удалены. Не восстанавливаю догадкой.

---

## Известное ограничение этого файла

Пока строки заполняет человек или чат, файл остаётся прозой о числах и способен
разойтись с кодом — тем же способом, каким разошёлся аудиторский пакет.
Предупреждение в тексте ограничителем не является. Планируется структурная проверка,
которая краснеет при расхождении числа в документе с выходом инструмента, вместе
с негативным контролем, доказывающим, что она способна покраснеть. До тех пор
единственная защита здесь — дисциплина, и это записано как слабое место, а не как решение.
